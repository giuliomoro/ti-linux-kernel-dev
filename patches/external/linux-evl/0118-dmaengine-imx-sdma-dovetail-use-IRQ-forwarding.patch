From 33fcd91d5a2127a4b9f2cc7a25948aac73a32950 Mon Sep 17 00:00:00 2001
From: Philippe Gerum <philippe.gerum@exail.com>
Date: Thu, 19 Oct 2023 09:06:36 +0200
Subject: [PATCH 0118/1010] dmaengine: imx-sdma: dovetail: use IRQ forwarding

Using irq_post_inband() in order to schedule a second callback of the
interrupt handler from the in-band stage proved to be broken, causing
double acknowledge of the interrupt event, which would yield IRQ loss
eventually.

Use inter-stage IRQ forwarding now available from the pipeline core
instead.

Signed-off-by: Philippe Gerum <philippe.gerum@exail.com>
---
 drivers/dma/imx-sdma.c | 24 ++++++++++++++----------
 1 file changed, 14 insertions(+), 10 deletions(-)

diff --git a/drivers/dma/imx-sdma.c b/drivers/dma/imx-sdma.c
index 9bb58572ad..67572f57fd 100644
--- a/drivers/dma/imx-sdma.c
+++ b/drivers/dma/imx-sdma.c
@@ -982,7 +982,7 @@ static unsigned long sdma_do_channels(struct sdma_engine *sdma,
 		__clear_bit(channel, &mask);
 	}
 
-	return stat;
+	return stat;	/* => channels pending in-band handling. */
 }
 
 static irqreturn_t sdma_int_handler(int irq, void *dev_id)
@@ -1006,17 +1006,21 @@ static irqreturn_t sdma_int_handler(int irq, void *dev_id)
 			sdma->pending_stat |= stat;
 			raw_spin_unlock(&sdma->oob_lock);
 			/* Call us back from in-band context. */
-			irq_post_inband(irq);
+			return IRQ_FORWARD;
 		}
-		return IRQ_HANDLED;
+	} else {
+		/* In-band IRQ context: stalled, but hard irqs are on. */
+		raw_spin_lock_irqsave(&sdma->oob_lock, flags);
+		stat = sdma->pending_stat;
+		sdma->pending_stat = 0;
+		raw_spin_unlock_irqrestore(&sdma->oob_lock, flags);
+		/*
+		 * Hybrid locking of oob-enabled vchans ensures that
+		 * hard IRQ are disabled across the locked section,
+		 * including from the in-band stage.
+		 */
+		sdma_do_channels(sdma, stat);
 	}
-
-	/* In-band IRQ context: stalled, but hard irqs are on. */
-	raw_spin_lock_irqsave(&sdma->oob_lock, flags);
-	stat = sdma->pending_stat;
-	sdma->pending_stat = 0;
-	raw_spin_unlock_irqrestore(&sdma->oob_lock, flags);
-	sdma_do_channels(sdma, stat);
 #else
 	stat = readl_relaxed(sdma->regs + SDMA_H_INTR);
 	writel_relaxed(stat, sdma->regs + SDMA_H_INTR);
-- 
2.30.2

